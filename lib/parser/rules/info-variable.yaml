rules:
  - id: info-function
    message: $CONTRACT | $SIG | $VIS | $PAY | $PURITY | $MOD | $OVER | $RETURN
    languages:
      - solidity
    severity: WARNING
    patterns:
      - pattern-inside: contract $CONTRACT { ... ... ... }
      - pattern: function $SIG() ...,  { ... }

